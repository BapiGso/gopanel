{{template "header.template"}}
<div class="text-base-content min-h-[60vh] flex items-center justify-center">
    <div class="text-center">
        <i class="ri-error-warning-line text-7xl opacity-20 mb-6 block"></i>
        <h3 class="text-2xl font-semibold mb-4">Feature Unavailable</h3>
        <p class="opacity-60 mb-8">Your operating system does not support this feature</p>
        <button onclick="history.back()" class="btn btn-primary">
            <i class="ri-arrow-left-line"></i>Go Back
        </button>
    </div>
</div>

<script>
    document.addEventListener('alpine:init', () => {
        Alpine.data('panel', () => ({
        }))
    })
</script>
{{template "footer.template"}}